# 프로젝트 개요: PeakBridge
본 프로젝트는 아파트 관제 시스템(`/frontend`) 및 VPP OS 콘솔(`/frontend-vpp`), 그리고 이를 지원하는 핵심 서버(`/backend`)로 구성되어 있습니다.
**프로젝트의 현재 완료 상태, 상세 아키텍처, 그리고 시연 시나리오는 반드시 `PARTNER_HANDOFF.md`(또는 해당 인수인계 문서)를 최우선으로 읽고 파악할 것.**

# 주요 디렉토리 구조 및 기술 스택
- `/frontend/`: 아파트 관제 프론트엔드 (React 기반)
- `/frontend-vpp/`: VPP OS 콘솔 (독립 앱, 포트 5180 구동)
- `/backend/`: 파이썬 백엔드 (수요예측, 시장 입찰, 급전 이행, Railway 자동 배포)

# 🚨 AI 코딩 필수 준수 규칙 (절대 어기지 말 것)
1. 프론트엔드 작업 시 함수형 컴포넌트를 사용하고, Props는 반드시 TypeScript 타입으로 명확히 정의할 것.
2. **백엔드(AI 예측 모듈) 수정 시 주의:** PPO 제어 등 AI 서빙은 `numpy` 로더로 교체되어 경량화된 상태임. 배포 환경을 무겁게 만드는 `SB3`나 `torch` 패키지를 절대 새로 설치하거나 requirements에 추가하지 말 것.
3. 시장/이행/원장 데이터는 백엔드 메모리(in-memory) 기반으로 동작함을 항상 인지하고, 서버 재시작 시 초기화된다는 점을 고려하여 코드를 짤 것.
4. 새로운 코드를 작성하기 전, 이것이 시연용 '원클릭 데모' 시퀀스나 기존 '시연 하이라이트 각본'에 영향을 주는지 반드시 검토할 것.
5. **사용자와의 모든 대화, 코드 주석, 설명은 반드시 한국어로 작성할 것.**