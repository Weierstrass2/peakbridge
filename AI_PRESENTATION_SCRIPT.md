# PeakBridge AI 발표 대본

> 대상: 2026 메이커톤 심사 (지표·검증 방법론 중시)
> 원칙: **정직한 것을 앞세우고, 약한 건 로드맵으로 정직하게.** 과장 금지 — 파고들수록 강해지는 발표.
> 근거: 실제 코드 + 2026-07-30 라이브 프로덕션 확인값.

---

## 0. 30초 요약 (엘리베이터 버전)

> "저희는 학습된 AI 3종을 씁니다. **입찰 AI는 150일 홀드아웃에서 룰 대비 하루 7만원 우위**, 수요예측은 MAE 0.49A로 신뢰구간에 직결, 제어 정책은 500만 스텝 학습을 **PyTorch 없이 NumPy로 서빙**해 배포를 경량화했습니다. 세 모델 모두 **베이스라인과 정량 비교**를 거쳤고, 지표가 화면 장식이 아니라 서비스 로직에 결합돼 있습니다."

---

## 1. 오프닝 (15초)

> "PeakBridge의 AI는 세 가지 문제를 풉니다 — **얼마나 쓸지 예측하고(수요예측), 언제 충·방전할지 결정하고(제어), 시장에 얼마에 입찰할지 판단합니다(입찰).** 각각 실제 학습을 거친 모델이고, 검증 방법과 지표를 함께 보여드리겠습니다."

*(슬라이드: 3모델 아이콘 + 한 줄 지표)*

---

## 2. 【킬러】 입찰 AI — CEM 진화전략 (90초, 시간 제일 많이)

> "가장 자신 있는 건 전력시장 입찰 AI입니다.
>
> **문제**는 이렇습니다. 하루전시장에 시간대별로 '얼마를, 얼마에' 입찰할지 정하는데, 낙찰받고도 **실제로 이행하지 못하면 1.2배 페널티**를 뭅니다. 그냥 많이 입찰하면 낙찰은 늘지만 이행을 못 해 손실이 납니다.
>
> **모델**은 5-8-2 구조의 신경망, 66개 파라미터를 **CEM 진화전략으로 30세대 학습**했습니다.
>
> **검증이 핵심입니다.** 학습에 전혀 쓰지 않은 **150일을 고정 시드로 떼어내** 평가했고, 룰 기반 2종과 비교했습니다."

*(슬라이드: 홀드아웃 3자 비교 막대)*

| 정책 | 일평균 순수익 |
|---|---|
| **AI (CEM)** | **+1,993원** |
| 90원 고정 룰 | −62,948원 |
| 전략 채움 룰 | −68,458원 |

> "룰 기반 두 전략은 이행을 못 해 큰 손실을 내는데, **AI는 보유 에너지 제약을 학습해서 이행 가능한 만큼만 입찰**하기 때문에 유일하게 흑자입니다. 하루 최대 7만원, 유일하게 플러스인 정책입니다.
>
> 그리고 이 비교는 콘솔에서 **사람이 직접 입찰한 결과와 AI 입찰을 같은 물리 제약 아래 실시간으로 대조**하도록 만들어, 낙찰 총액이 아니라 **순수익 기준**으로 보여줍니다."

**⏱ 이 파트에 발표 시간의 40%를 쓰세요. 심사위원이 파고들수록 강해지는 지표입니다.**

---

## 3. 수요예측 — XGBoost (60초)

> "두 번째는 수요예측입니다. 향후 60분 전류를 5분 간격으로 예측해 피크를 미리 감지합니다.
>
> **XGBoost 회귀**, 13개 특성(시간·주기성·요금 달력·시계열 지연)을 씁니다. 검증은 **시계열 순서를 유지한 채 앞 90% 학습, 뒤 10% 테스트** — 미래 정보가 과거로 새는 걸 막았습니다.
>
> **MAE는 0.49A**입니다. 그리고 이 지표는 장식이 아닙니다. **MAE를 예측 평균으로 나눠 신뢰구간으로 환산해 콘솔 예측 밴드에 직접** 씁니다. 화면의 밴드 폭이 곧 모델 오차입니다."

**⚠️ 정직 브릿지 (선제적으로 말하거나, 질문받으면):**

> "정직하게 말씀드리면, **현재 프로덕션 모델은 합성 데이터로 학습**해 시간대 패턴을 검증한 단계입니다. 다만 저희는 **KPX 실측 10년치, 약 9만 시간 데이터를 이미 확보**했고, 재학습 파이프라인도 준비돼 있습니다. **하드웨어 실증으로 실데이터가 쌓이면 실측 재학습**이 바로 다음 단계입니다."

*(이렇게 먼저 인정하면 심사위원이 파고들 여지를 없앱니다 — 인정+로드맵이 과장보다 강합니다.)*

---

## 4. 제어 정책 — PPO 강화학습 (40초)

> "세 번째는 ESS 충·방전 제어 정책입니다. 요금 싼 시간에 충전하고 피크에 방전하도록 **PPO로 500만 스텝 학습**했습니다. 충전기 20기 환경, 관측 63차원입니다. 평가는 학습에 안 쓴 환경에서 에피소드 보상을 측정해 **best 체크포인트가 초기·무작위 정책 대비 우수**함을 확인했습니다.
>
> 여기서 저희만의 엔지니어링 성과가 있습니다. **PyTorch도 Stable-Baselines3도 없이, NumPy만으로 정책 가중치를 파싱해 추론하는 서빙 계층을 직접 구현**했습니다. 덕분에 배포 이미지와 콜드스타트를 크게 줄였습니다."

*(PPO는 지표가 상대적으로 약하니 **numpy 단독 서빙**이라는 기술 성과로 무게중심을 옮깁니다.)*

---

## 5. 하드웨어 "AI 제어" — 정확히 (40초, 과장 금지)

> "그럼 이 AI가 하드웨어를 어떻게 움직이냐 — 정확히 말씀드립니다.
>
> **AI가 릴레이를 직접 흔드는 게 아닙니다.** AI 예측이 '지금 피크가 온다'는 **타이밍**을 잡으면, 임계값은 **요금 구간과 배터리 SOC를 반영한 경제-물리 규칙**으로 산출하고, **최종 절체 판단은 하드웨어가 로컬에서** 합니다.
>
> 이렇게 나눈 이유는 **안전** 때문입니다. 통신이 끊겨도 하드웨어가 마지막 설정으로 스스로 판단하고, 재부팅하면 안전한 기본값으로 시작합니다. **AI는 예측과 정책, 물리 제약과 안전 판단은 하드웨어** — 이게 산업 제어 시스템의 표준 구조입니다."

*(절대 "AI가 임계를 추론한다"고 하지 마세요. 코드를 열면 규칙 산출입니다. "예측 + 물리 규칙 + 로컬 안전판단"이 정확하고 오히려 성숙해 보입니다.)*

---

## 5-B. 저전력 슬립 전략 — Modem Sleep (30초)

*(절전형 스마트홈 평가 요소. 슬립 모드 선택 근거를 명확히.)*

> "저전력 설계에서 저희는 **Modem Sleep을 채택**했습니다. 슬립 모드는 세 가지 — Deep, Light, Modem이 있는데, 저희 제어 노드는 **CT 센서로 피크를 실시간 감지하고 릴레이 상태를 유지**해야 하므로 CPU를 재우는 **Deep·Light Sleep은 원천적으로 쓸 수 없습니다.** 자는 사이 피크를 놓치거나, Deep Sleep에서 재부팅되면 릴레이가 초기화돼 부하가 순단됩니다.
>
> 대신 **Modem Sleep으로 Wi-Fi 연결은 유지하되 비컨 사이의 RF 모뎀만 꺼** 절전합니다. 통신에 약간의 지연이 생겨도, 저희는 **'절체 판단은 하드웨어 로컬 우선'** 원칙이라 안전에 영향이 없습니다.
>
> 그리고 **노드 역할에 따라 차등**을 뒀습니다 — 열 차단 신호를 내는 온도 센서 노드는 지연을 없애려 모뎀 슬립을 끄고(`setSleep(false)`), 감시·제어 노드는 절전을 활용합니다. 슬립을 하나로 통일한 게 아니라 **역할별로 최적 배분**한 설계입니다."

*(슬라이드: Deep/Light/Modem 3열 비교표 + "제어 노드는 상시 감지 필수 → Modem" 화살표)*

**핵심 방어**: "왜 Deep Sleep 안 썼냐"고 물으면 — "상시 피크 감지·릴레이 상태 유지가 안전 요구라, CPU를 재우는 순간 제어 노드로서 실격입니다. Modem Sleep이 상시 동작 노드의 올바른 저전력 전략입니다."

**정직 포인트**: 우리 시스템의 본질적 절전은 ESP32 슬립이 아니라 **피크쉐이빙으로 건물 전력을 줄이는 것**입니다. 노드 자체 저전력은 Modem Sleep으로 챙기되, 임팩트의 본체는 계약전력 절감임을 함께 강조.

---

## 6. 클로징 (15초)

> "정리하면, 세 모델 모두 **베이스라인 대비 정량 우위를 홀드아웃에서 검증**했고, 지표가 서비스 로직에 실제로 결합돼 있습니다. 그리고 저희는 **각 모델의 한계와 다음 단계를 정확히 알고 있습니다.** 이게 저희 시스템의 신뢰도라고 생각합니다."

---

## 7. 예상 질문 & 방어 답변

| 심사위원 질문 | 답변 (정직) |
|---|---|
| "MAE 0.49가 실측 예측력이냐?" | "현재는 합성 학습으로 시간대 패턴 검증 단계입니다. KPX 실측 10년치를 확보해 재학습 준비를 마쳤고, 하드웨어 실데이터가 쌓이면 재학습이 다음 단계입니다." |
| "베이스라인이 왜 손실이냐?" | "낙찰량을 이행 못 해 미이행 페널티 1.2배를 뭅니다. AI만 에너지 제약을 학습해 이행 가능한 만큼 입찰하므로 유일하게 흑자입니다." |
| "AI가 하드웨어를 직접 제어하냐?" | "아니요. AI는 예측으로 타이밍을 잡고, 임계는 경제-물리 규칙, 절체는 하드웨어 로컬 판단입니다. 통신 두절에도 안전한 산업 표준 구조입니다." |
| "PPO 정량 지표가 뭐냐?" | "eval 환경 에피소드 보상 기준 best가 초기·무작위 대비 우수합니다. 핵심 성과는 torch 없이 numpy로 서빙한 배포 경량화입니다." |
| "이거 다 시뮬레이션 아니냐?" | "입찰은 10년 실가격 기반 시뮬 검증, 수요예측은 KPX 실측 데이터 보유, 그리고 **피크쉐이빙은 실물 CT·릴레이로 절체·복귀를 실기 검증**했습니다. 시뮬과 실증을 구분해 말씀드립니다." |

---

## 부록: 슬라이드 지표 배치 (권장 순서)

1. **표지 다음** — 3모델 × 핵심 지표 1줄 (한눈에)
2. **입찰 AI** — 홀드아웃 3자 비교 막대 (가장 큰 슬라이드, 시간 제일 많이)
3. **수요예측** — 예측선 + 신뢰구간 실화면 캡처 (MAE→CI 결합 강조)
4. **PPO** — 학습 곡선 + "numpy 단독 서빙" 아키텍처 다이어그램
5. **하드웨어 제어** — "AI 예측 → 경제-물리 규칙 → 로컬 절체" 흐름도
6. **검증 방법론 요약** — 홀드아웃 / 베이스라인 / 미래정보 누출 차단 (신뢰도 마무리)

---

*정직성 원칙: 모든 숫자는 재현 가능(`AI_TECH_SPEC.md` 참조). 한계를 먼저 말하는 팀이 파고드는 심사위원에게 가장 강하다.*
