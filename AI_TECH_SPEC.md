# PeakBridge AI 시스템 기술 명세서

> 대상: 2026 AI 기반 절전형 스마트홈 메이커톤 / 팀 PeakBridge
> 범위: **관제 대시보드 2종**(아파트 관제 `/app`, VPP OS 콘솔 `/console`)에 탑재된 AI/최적화 모델 전체
> 원칙: 실제 코드·라이브 프로덕션 응답을 근거로 작성. 과장 없이 한계까지 명시(심사 방어력 우선).
> 최종 확인일: 2026-07-30 (라이브 프로덕션 직접 조회)

---

## 0. 한눈에 보기 — 대시보드 × AI 매핑

PeakBridge에는 **학습된 AI 3종 + 규칙기반 최적화기 1종**이 있고, 두 대시보드가 이를 나눠 쓴다.

| # | 엔진 | 유형 | 아파트 관제 `/app` | VPP 콘솔 `/console` |
|---|---|---|:---:|:---:|
| ① | XGBoost 수요예측 | 지도학습(회귀) | ✅ AI 피크 예측 패널 | ✅ 예측선·신뢰구간 |
| ② | PPO 충방전 제어 | 강화학습 | ✅ `/ai/recommend` 제어 권고 | — |
| ③ | CEM 입찰 정책 | 진화전략 | — | ✅ 입찰 데스크 |
| ④ | EnergyOptimizer | **규칙기반(AI 아님)** | ✅ 요금 최적 스케줄 | — |

> **정직 포인트**: ④는 학습 모델이 아니라 한국 전기요금표(계절·시간대) 기반 결정 규칙이다. 본 문서는 이를 "AI"로 표기하지 않는다.

추가로, 이 AI들의 출력을 소비하는 **하드웨어 폐루프 제어**가 있다:
- **오토파일럿**(`hardware/server/autopilot.py`): ①의 예측(next_peak)을 폴링해 하드웨어 절체 임계를 선제 하향. AI 자체가 아니라 ①의 예측을 소비하는 제어 루프다.

---

## 1. 대시보드별 AI 구성

### 1-A. 아파트 관제 대시보드 (`/app`, 단지 관리자용)

| 화면/기능 | 사용 엔진 | API |
|---|---|---|
| AI 피크 예측 패널 (다음 피크 배지, 예측 카드) | ① XGBoost | `GET /api/v1/dashboard/{id}` (`next_peak`, `forecast`) |
| ESS 제어 권고 | ② PPO | `GET /api/v1/ai/recommend/{id}` |
| 에너지 거래/충방전 스케줄 | ④ EnergyOptimizer | `GET /api/v1/energy/recommendation/{id}` |
| 하드웨어 선제 절체 (실증) | ① 예측 → 오토파일럿 | XGBoost `next_peak` → MQTT config |

### 1-B. VPP OS 콘솔 (`/console`, 사업자/트레이더용)

| 화면/기능 | 사용 엔진 | API |
|---|---|---|
| 실시간 스트림 예측선·신뢰구간 | ① XGBoost | `GET /api/v1/vpp/stream` (`forecast_kw`, `forecast_hi/lo`, `ci_ratio`, `forecast_source`) |
| 입찰 데스크 (AI 입찰 채움) | ③ CEM 입찰 | `GET /api/v1/market/ai-bids` |
| 섀도우 정산 비교 | ③ CEM 입찰 | 사람 입찰 vs AI 입찰 기회비용 |

---

## 2. 모델별 상세 명세

### ① XGBoost 수요예측 모델

| 항목 | 내용 |
|---|---|
| **목적** | 향후 60분 전류 수요를 5분 간격(12스텝) 예측 → 피크 사전 감지, 예측선·신뢰구간 생성 |
| **알고리즘** | XGBoost 회귀 (`XGBRegressor`) |
| **하이퍼파라미터** | n_estimators 300 / max_depth 6 / learning_rate 0.05 / subsample 0.8 / colsample_bytree 0.8 / seed 42 |
| **입력 특성(13개)** | hour, minute, weekday, is_weekend, is_holiday, season, hour_sin, hour_cos, month, is_peak_hour, lag_1h, lag_24h, rolling_mean_3h |
| **평가 방법** | 시계열 순서 유지 90%/10% 분리 (shuffle 금지 — 미래 정보 누출 방지) |
| **산출물** | `backend/app/ml/xgboost_forecaster.py`, 학습 `backend/scripts/train_xgboost.py` |

**성능 지표 (2026-07-30 라이브 프로덕션 직접 조회)**

| 지표 | 값 | 조회 경로 |
|---|---|---|
| **MAE** | **0.494 A** | `GET /api/v1/energy/model-info` |
| **RMSE** | **0.615 A** | 동일 |
| 신뢰구간 비율 ci_ratio | 0.0468 (≈4.7%) | `GET /api/v1/vpp/stream` |

- 신뢰구간은 `MAE ÷ 예측평균`으로 환산(2~8% 클램프)해 콘솔 예측 밴드에 직접 사용된다 — 장식 지표가 아니라 서비스 로직에 결합.
- `forecast_source: "xgboost"`는 실제 모델이 로드·추론 중임을 뜻한다(로드 실패 시 `"sim"`).

**⚠️ 데이터 출처 — 반드시 정확히 이해할 것 (심사 방어의 핵심)**

이 모델에는 **두 개의 데이터 경로**가 있고, 현재 프로덕션은 그중 합성 경로를 쓴다.

| 경로 | 데이터 | 규모 | 현재 사용? |
|---|---|:---:|:---:|
| 실측 | `backend/data/kpx_processed.csv` (KPX 실측 전력수요) | **91,920시간, 2016-01-02 ~ 2026-06-27** | ❌ 미사용 |
| 합성 | `forecaster.generate_synthetic_data()` (규칙기반 생성) | 14일, 5분 간격 | ✅ **프로덕션 사용** |

- 프로덕션(`main.py`)은 배포 시 `/tmp/models`에 모델이 없으면 **합성 데이터로 자동 학습**한다. `/tmp`라 재배포마다 초기화 → 매번 합성 재학습.
- 합성 데이터 구조: `seed=42` 고정, 평일 base 10.0A / 주말 8.0A, 저녁(18–21시) +6~9A, 아침(7–9시) +3A, 점심(12–13시) +2A, 노이즈 N(0, 0.8).
- **따라서 MAE 0.494A는 "합성 데이터가 합성 규칙을 재현한" 오차이며, 현실 예측력이 아니다.** 실측 평균 전류 9.295A 대비 5.3%로 숫자는 준수하나, 테스트셋 자체가 학습과 동일한 규칙에서 나왔다.
- 이 때문에 아파트 대시보드(`dashboard.py`)는 XGBoost 원값을 그대로 쓰지 않고 **명시적 시간대 배율로 예측을 재구성**한다(합성 lag가 더미 고정이라 시간대 반응이 약함). `get_model_info`가 반환하는 `"training_data": "KPX 한국전력거래소 실제 수요 데이터"`는 **코드에 하드코딩된 라벨**로, 실제 프로덕션 학습 데이터와 일치하지 않는다.

> **로드맵**: 실측 `kpx_processed.csv`(10년치)와 하드웨어 실측 축적분으로 재학습하면 "진짜 예측력"으로 전환된다. 라즈베리파이 상시 가동으로 실측을 축적하는 것이 다음 단계다.

---

### ② PPO 충방전 제어 모델 (강화학습)

| 항목 | 내용 |
|---|---|
| **목적** | ESS(충전기 묶음)의 충·방전 정책 — 요금 싼 시간 충전, 피크 시간 방전 |
| **알고리즘** | PPO (Proximal Policy Optimization, Stable-Baselines3) |
| **학습 환경** | EV2Gym 시뮬레이터 `PublicPST` 시나리오, 충전기 20기, **관측 63차원 / 행동 20차원(연속)** |
| **학습량** | 총 5,000,000 스텝 / 병렬 환경 4 / lr 3e-4 / n_steps 2048 / 40만 스텝 간격 체크포인트 13개 |
| **모델 선택** | EvalCallback이 5만 스텝마다 평가 → 최고 성능을 `best_model.zip`(440만 스텝 시점) 자동 보존 |
| **평가 방법** | 학습에 안 쓴 eval_env 에피소드 보상 측정, ⓐ best vs ⓑ 초기 체크포인트 vs ⓒ 무작위 정책 비교 |
| **결과** | best 체크포인트가 초기·무작위 대비 우수 보상 → `models/best/best_model.zip` 채택 |
| **산출물** | 서빙 `app/services/ppo_service.py`, 로더 `app/ml/np_policy.py`, 학습 `scripts/train_ev2gym_ppo.py` |

**기술적 성과 — NumPy 단독 서빙**
- 배포 경량화를 위해 **PyTorch·SB3 없이 NumPy만으로 정책 가중치를 로드·추론**하는 계층을 자체 구현(`np_policy.py`의 `NumpyPPOPolicy`). SB3 zip 아카이브를 직접 파싱(`_Unpickler`)해 가중치를 복원 → 배포 이미지·콜드스타트 대폭 절감.
- ⚠️ 프로젝트 규칙: **SB3/torch를 requirements에 다시 추가 금지**(numpy 로더로 대체된 상태).

**실제 서비스 반영** — `GET /api/v1/ai/recommend/building-A`
```json
{ "model": "PPO (5M steps)", "action": "discharge", "recommended_rate": 58,
  "reason": "PPO 충전 억제 신호 + 그리드 임계 근접 — ESS 방전 권장",
  "grid_current": 19.95, "ess_soc": 25.0 }
```

**평가지표 요약**: 에피소드 누적 보상(eval_env) 기준 best > 초기 > 랜덤. 정량 절대값보다 **정책 개선의 방향성**을 검증하는 방식.

**한계**: EV2Gym 표준 시나리오(충전기 20기)로 학습된 정책을 실증 단지 규모에 어댑터로 매핑해 사용한다.

---

### ③ CEM 입찰 정책 모델 (진화전략)

| 항목 | 내용 |
|---|---|
| **목적** | 전력 하루전시장(DAM) 시간대별 입찰량·입찰가 제출 — 이행 가능 에너지·미이행 페널티까지 고려한 순수익 최대화 |
| **알고리즘** | CEM (Cross-Entropy Method) 진화전략 |
| **모델 구조** | 5-8-2 다층 퍼셉트론, **총 66 파라미터** (입력: 시간대별 예측가·수요지표 / 출력: 입찰량·입찰가) |
| **학습 방법** | 세대당 28 후보 → 상위 7 엘리트의 평균·표준편차로 분포 갱신, **30세대** 반복. 세대별 학습셋에서 무작위 10일 평가 |
| **학습 데이터** | 10년치 실제 시장가격 기반 시뮬(요금 캘린더 + KPX 수요 백분위 희소성 프리미엄) |
| **산출물** | 서빙 `app/services/bid_ai.py`, 가중치 `models/bid_policy.json`(평가결과 내장), 학습 `scripts/train_bid_policy.py` |

**성능 지표 (150일 홀드아웃 검증, `bid_policy.json` 내장값)**

학습에 안 쓴 **150일**을 고정 시드로 추출, 일평균 순수익 3자 비교:

| 정책 | 일평균 순수익 | AI 대비 |
|---|---|---|
| **AI 정책 (CEM)** | **+₩1,993** | — |
| 90원 고정 룰 | −₩62,948 | AI가 **₩64,941** 우위 |
| 전략채움 룰 | −₩68,458 | AI가 **₩70,451** 우위 |

- 베이스라인이 큰 손실을 내는 이유: 낙찰량을 실제로 이행하지 못해 **미이행 페널티(1.2배)** 부담. AI는 보유 에너지 제약을 학습해 이행 가능한 만큼만 입찰 → 유일하게 흑자.
- **섀도우 입찰 비교**: 콘솔 입찰 데스크에서 사람 입찰과 AI 입찰을 **동일 물리 제약(에너지 예산+페널티)** 하에 순수익 기준으로 비교(낙찰 총액만 보면 과다입찰 착시 발생).

**한계**: 평가 환경은 과거 가격 시뮬이며, 실제 시장 유동성·경쟁 입찰자 반응은 모사하지 않는다.

---

### ④ EnergyOptimizer (규칙기반 — AI 아님, 명시)

| 항목 | 내용 |
|---|---|
| **목적** | 한국 전기요금표(계절·시간대) 기반 최적 충방전 시점 안내 |
| **방식** | **결정 규칙** — 요금표(경부하/중간/최대부하 × 여름/겨울/봄가을), 공휴일·토요일 특례. 학습 없음 |
| **산출물** | `app/ml/energy_optimizer.py` |
| **평가지표** | 해당 없음(규칙기반). 요금 절감액을 사후 계산 |

> 본 엔진은 명시적으로 "AI"가 아니다. 심사에서 AI로 소개하지 않는다. 요금 최적화 로직으로만 설명한다.

---

## 3. 벤치마킹 지표 종합표

| 엔진 | 핵심 지표 | 값 | 베이스라인 대비 | 검증 방식 |
|---|---|---|---|---|
| ① XGBoost | MAE / RMSE | 0.494A / 0.615A | 신뢰구간 산출에 직결 | 시계열 90/10 홀드아웃 |
| ② PPO | 에피소드 보상 | best > 초기 > 랜덤 | 무작위·초기 대비 우수 | eval_env 주기 평가 |
| ③ CEM 입찰 | 일평균 순수익 | +₩1,993 | 룰 대비 **+₩64,941 ~ +₩70,451/일** | 150일 홀드아웃 3자 비교 |
| ④ Optimizer | (규칙) | — | — | — |

**공통 강점**: 세 AI 모두 지표가 데모용 장식이 아니라 서비스 로직에 결합(① MAE→신뢰구간, ② 보상→제어 권고, ③ 순수익→섀도우 기회비용).

---

## 4. 데이터 출처 종합

| 엔진 | 데이터 출처 | 구조 | 실측/합성 |
|---|---|---|---|
| ① XGBoost (실측경로) | KPX 전력거래소 `kpx_processed.csv` | 91,920시간 시계열(2016–2026), ds·y·13특성 | **실측(현재 미사용)** |
| ① XGBoost (프로덕션) | `generate_synthetic_data()` | 14일 5분 간격, 규칙 생성 | **합성(현재 사용)** |
| ② PPO | EV2Gym `PublicPST` 시뮬 | 관측 63차원 / 행동 20차원 | 시뮬 |
| ③ CEM 입찰 | 10년 시장가격 시뮬(요금캘린더×KPX수요백분위) | 시간대별 가격·수요 지표 | 실측기반 시뮬 |
| SMP(참고) | KPX 당일 확정가 웹 크롤링 | 24h 시간별, 6h 주기 중계 | 실측 |

---

## 5. 종합 평가 및 정직한 한계

**강점**
- 세 AI 모두 홀드아웃/eval셋에서 **베이스라인 대비 정량 우위** 확인.
- 지표가 서비스에 실제 결합. PPO를 NumPy 단독 추론으로 서빙해 배포 경량화 달성.
- 절체 판단은 하드웨어 로컬, AI는 예측·권고·입찰만 — 안전 원칙 분리.

**한계 (정직한 기술)**
- **① XGBoost는 현재 프로덕션이 합성 데이터로 학습**된다. MAE 0.494A는 합성 자기재현 오차이며, `training_data` 라벨("KPX 실측")은 하드코딩값으로 실제와 불일치한다. 실측 `kpx_processed.csv`(10년치)가 이미 있으므로 재학습이 다음 단계다.
- ② PPO는 EV2Gym 표준 시나리오(20기)를 실증 규모에 어댑터 매핑.
- ③ CEM 평가는 과거 가격 시뮬로, 경쟁 입찰자·유동성 미모사.
- ④ EnergyOptimizer는 규칙기반(비-AI).

**개선 로드맵**
1. **XGBoost 실측 재학습** — `kpx_processed.csv` + 하드웨어 상시 축적분으로 재학습, `training_data` 라벨 정정.
2. **엣지 자율화** — 오토파일럿의 예측을 라즈베리파이 로컬 XGBoost 추론으로 전환(클라우드 두절 내성).
3. **PPO 실증 스케일 재학습** — 실증 단지 규모 환경으로 재학습.

---

## 6. 검증 재현 / 라이브 확인

**학습 재현**
| 모델 | 명령 |
|---|---|
| ① XGBoost | `python backend/scripts/train_xgboost.py` (실측 `kpx_processed.csv` 사용) |
| ② PPO | `python backend/scripts/train_ev2gym_ppo.py` |
| ③ CEM 입찰 | `python backend/scripts/train_bid_policy.py` |

**라이브 즉시 확인**
```
GET /api/v1/energy/model-info                  → XGBoost MAE/RMSE
GET /api/v1/vpp/stream                          → forecast_source, ci_ratio
GET /api/v1/ai/recommend/building-A             → PPO 추론 결과
GET /api/v1/market/ai-bids                      → CEM 입찰 정책 출력
```
(호스트: `https://peakbridge-production.up.railway.app`)

---

*본 문서는 실제 코드와 2026-07-30 라이브 프로덕션 응답을 근거로 작성되었으며, 기존 `AI_BENCHMARK_REPORT.md`를 대시보드 매핑·데이터 출처 정직성 관점에서 확장·정정한 것이다.*
