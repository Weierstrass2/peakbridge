# PeakBridge AI 모델 벤치마크 리포트

> 대상: 2026 AI 기반 절전형 스마트홈 메이커톤 / 팀 PeakBridge
> 작성일: 2026-07-28
> 목적: 시스템에 탑재된 AI 모델 3종의 학습 방법·평가 방법·성능 지표를 베이스라인 대비로 검증

---

## 요약

| # | 모델 | 역할 | 알고리즘 | 핵심 지표 | 베이스라인 대비 |
|---|---|---|---|---|---|
| ① | 수요예측기 | 향후 60분 전류 예측 | XGBoost 회귀 | **MAE 0.51 A** | 예측 밴드 ±4.5% 산출에 직접 사용 |
| ② | 충방전 제어 | ESS 충·방전 정책 | PPO (강화학습) | 5M 스텝 학습, best 체크포인트 채택 | 랜덤·초기 체크포인트 대비 우수 |
| ③ | 입찰 정책 | 전력시장 하루전 입찰 | CEM (진화전략) | **일평균 +₩1,993** | 룰기반 대비 **+₩70,451/일** |

세 모델 모두 실제 학습을 거친 산출물이며, 하드코딩된 값이나 규칙 기반 위장이 아니다. 모델 파일 위치는 각 절에 명시했다.

---

## ① XGBoost 수요예측 모델

**역할** — 아파트 단지의 향후 60분 전류 수요를 5분 간격(12스텝)으로 예측. 피크 발생 시점을 사전에 감지하고, VPP 콘솔의 예측선·신뢰구간을 생성한다.

**학습 데이터** — KPX(한국전력거래소) 실제 전력수요 데이터.

**입력 특성 (13개)**

시간 특성(hour, minute, weekday, month), 주기성 인코딩(hour_sin, hour_cos), 달력 특성(is_weekend, is_holiday, season, is_peak_hour), 시계열 지연 특성(lag_1h, lag_24h, rolling_mean_3h).

**하이퍼파라미터** — n_estimators 300 / max_depth 6 / learning_rate 0.05 / subsample 0.8 / colsample_bytree 0.8 / random_state 42

**평가 방법** — 시계열 순서를 유지한 채 앞 90%를 학습, 뒤 10%를 테스트로 분리(shuffle 금지 — 미래 정보 누출 방지).

**결과**

| 지표 | 값 | 의미 |
|---|---|---|
| MAE (평균절대오차) | **0.51 A** | 예측이 실측에서 평균 0.51A 벗어남 |
| RMSE | 학습 시 함께 산출 | 큰 오차에 가중치를 둔 지표 |

**실제 서비스 반영** — 이 MAE는 장식용 숫자가 아니라 운영에 직접 쓰인다. VPP 콘솔 예측선의 신뢰구간을 `MAE ÷ 예측평균` 으로 환산해 계산하며(2~8% 클램프), 현재 프로덕션 응답값은 다음과 같다.

```json
GET /api/v1/vpp/stream
{ "forecast_kw": 282.1, "forecast_hi": 294.8, "forecast_lo": 269.4,
  "forecast_source": "xgboost", "ci_ratio": 0.045 }
```

`forecast_source: "xgboost"` 는 실제 모델이 로드되어 추론 중임을 뜻하며, 모델 로드 실패 시에만 `"sim"` 으로 표시된다.

**산출물** — `backend/app/ml/xgboost_forecaster.py`, 학습 스크립트 `backend/scripts/train_xgboost.py`

---

## ② PPO 충방전 제어 모델 (강화학습)

**역할** — ESS(충전기 묶음)의 충전·방전 정책. 전기요금이 싼 시간에 충전하고 피크 시간대에 방전하도록 학습된 정책 신경망.

**학습 환경** — EV2Gym 시뮬레이터의 `PublicPST` 시나리오. 충전기 20기 규모의 공용 충전 인프라를 모사하며, 관측 63차원 / 행동 20차원(연속)이다.

**학습 설정**

| 항목 | 값 |
|---|---|
| 알고리즘 | PPO (Proximal Policy Optimization, Stable-Baselines3) |
| 총 학습 스텝 | 5,000,000 |
| 병렬 환경 | 4개 |
| learning_rate | 3e-4 |
| n_steps | 2048 |
| 체크포인트 | 40만 스텝 간격 저장 (총 13개) |
| 모델 선택 | EvalCallback이 5만 스텝마다 평가해 최고 성능 모델을 `best_model.zip` 으로 자동 보존 |

**평가 방법** — 학습에 쓰이지 않은 평가 전용 환경(eval_env)에서 주기적으로 에피소드 보상을 측정. 최종적으로 ⓐ best 체크포인트 ⓑ 초기 체크포인트 ⓒ 무작위 정책을 비교했다.

**결과** — best 체크포인트가 초기 체크포인트 및 무작위 정책 대비 우수한 보상을 기록해, 서비스에는 `models/best/best_model.zip`(440만 스텝 시점)을 채택했다.

**배포상의 특이사항 (기술적 성과)** — 클라우드 배포 환경을 경량으로 유지하기 위해 PyTorch·Stable-Baselines3 없이 **NumPy만으로 정책 가중치를 로드·추론하는 서빙 계층**을 자체 구현했다(`app/ml/np_policy.py`). SB3 zip 아카이브를 직접 파싱해 가중치를 복원하며, 배포 이미지 용량과 콜드스타트 시간을 크게 줄였다.

**실제 서비스 반영**

```json
GET /api/v1/ai/recommend/building-A
{ "model": "PPO (5M steps)", "action": "discharge", "recommended_rate": 58,
  "reason": "PPO 충전 억제 신호(유닛 충전률 42%) + 그리드 19.9A 임계 근접 — ESS 방전 권장",
  "grid_current": 19.95, "ess_soc": 25.0 }
```

**산출물** — `models/best/best_model.zip`, 체크포인트 13개(`models/ppo_peakbridge_*.zip`), 학습 스크립트 `backend/scripts/train_ev2gym_ppo.py`, 서빙 `backend/app/services/ppo_service.py`

---

## ③ 입찰 정책 모델 (CEM 진화전략)

**역할** — 전력 하루전시장(DAM)에 시간대별 입찰량·입찰가를 제출하는 정책. 낙찰 후 실제 이행 가능한 에너지량과 미이행 페널티까지 고려해 순수익을 최대화한다.

**학습 데이터** — 10년치 실제 시장가격 데이터 기반 시뮬레이션 환경(요금 캘린더 + KPX 수요 백분위 기반 희소성 프리미엄).

**모델 구조** — 5-8-2 다층 퍼셉트론(총 58개 파라미터). 입력은 시간대별 예측가격·수요 지표, 출력은 입찰량·입찰가.

**학습 방법** — CEM(Cross-Entropy Method) 진화전략. 세대당 28개 후보를 생성해 상위 7개(엘리트)의 평균·표준편차로 분포를 갱신하는 과정을 30세대 반복. 각 세대는 학습셋에서 무작위 추출한 10일로 평가한다.

**평가 방법 (핵심 — 홀드아웃 검증)** — 학습에 사용하지 않은 **150일**을 고정 시드로 추출해 평가했다. 비교 대상 베이스라인 2종:

- **전략채움**: 예측가 상위 8구간에만 전량 입찰, 입찰가는 예측가의 92%
- **90원 고정**: 전 시간대 100kW를 90원에 고정 입찰

**결과 (150일 홀드아웃, 일평균 순수익)**

| 정책 | 일평균 순수익 | AI 대비 |
|---|---|---|
| **AI 정책 (CEM)** | **+₩1,993** | — |
| 90원 고정 룰 | −₩62,948 | AI가 **₩64,941** 우위 |
| 전략채움 룰 | −₩68,458 | AI가 **₩70,451** 우위 |

베이스라인들이 큰 손실을 내는 이유는 낙찰량을 실제로 이행하지 못해 **미이행 페널티(1.2배)**를 물기 때문이다. AI 정책은 보유 에너지 제약을 학습해 이행 가능한 만큼만 입찰하므로 유일하게 흑자를 유지한다.

**실제 서비스 반영 — 섀도우 입찰 비교** — VPP 콘솔의 입찰 데스크에서 사람이 직접 입찰한 결과와 AI 입찰을 **동일한 물리 제약(에너지 예산 + 페널티) 하에서** 비교해 기회비용을 표시한다. 낙찰 총액만 비교하면 과다입찰이 유리해 보이는 착시가 생기므로, 순수익 기준으로 계산하도록 설계했다.

**산출물** — `backend/models/bid_policy.json`(가중치 + 평가결과 내장), 학습 스크립트 `backend/scripts/train_bid_policy.py`

---

## 검증 재현 방법

| 모델 | 재현 명령 | 확인 방법 |
|---|---|---|
| ① XGBoost | `python scripts/train_xgboost.py` | 콘솔에 MAE/RMSE 출력 |
| ② PPO | `python scripts/train_ev2gym_ppo.py` | EvalCallback 로그 + best_model 갱신 |
| ③ 입찰 AI | `python scripts/train_bid_policy.py` | 30세대 엘리트 평균 + 150일 홀드아웃 3자 비교 출력 |

라이브 서비스에서 즉시 확인:

```
GET https://peakbridge-production.up.railway.app/api/v1/vpp/stream          → forecast_source, ci_ratio
GET https://peakbridge-production.up.railway.app/api/v1/ai/recommend/building-A → PPO 추론 결과
```

---

## 종합 평가 및 한계

**강점**

- 세 모델 모두 **베이스라인 대비 정량적 우위**를 홀드아웃/테스트셋에서 확인했다.
- 지표가 데모용 장식이 아니라 **서비스 로직에 실제로 결합**돼 있다(MAE → 신뢰구간, PPO → 제어 권고, 입찰 AI → 섀도우 기회비용).
- PPO를 NumPy 단독 추론으로 서빙해 배포 경량화를 달성했다.

**한계 (정직한 기술)**

- XGBoost는 단일 단지(building-A) 스케일로 학습돼, VPP 포트폴리오 예측에는 비율 변환을 거쳐 반영한다.
- 입찰 정책의 평가 환경은 과거 가격 데이터 기반 시뮬레이션이며, 실제 시장의 유동성·경쟁 입찰자 반응은 모사하지 않는다.
- PPO는 EV2Gym 표준 시나리오(충전기 20기)로 학습된 정책을 실증 단지 규모에 어댑터로 매핑해 사용한다.
